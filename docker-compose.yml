version: '3'
services:
  shadowsocks:
    image: hopher/shadowsocks-libev:latest
    container_name: chat
    ports:
    
      - "8080:8080/udp"
    build:
      context: .
    environment:
      - METHOD=rc4 # 加密方法
      - PASSWORD=170826  # 密码
    restart: always